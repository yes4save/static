/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://yes4save.org', // change to your live domain
  generateRobotsTxt: true, // also generates robots.txt
  generateIndexSitemap: false,
  sitemapSize: 7000,
};
